services:
  - type: web
    name: campus-connect-fd
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_BASE_URL
        value: https://campus-connect-higr.onrender.com
